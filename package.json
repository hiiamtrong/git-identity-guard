{
  "name": "git-identity-guard",
  "version": "0.1.1",
  "description": "Git, GitHub, Claude Code, Codex CLI, and Pi identity enforcement",
  "type": "module",
  "files": ["bin", "scripts", "hooks", "skills", "extensions", ".claude-plugin", ".codex-plugin"],
  "scripts": {
    "test": "node --experimental-strip-types --test test/*.test.ts && ./tests/test_git_identity_guard.sh",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.0.0"
  },
  "pi": {
    "extensions": ["./extensions/git-identity-guard-required.ts"],
    "skills": ["./skills"]
  }
}
